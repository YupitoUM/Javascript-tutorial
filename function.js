export const capitalizeString = (str) => str.toUpperCase();
export const amor = "Yupito y Guisella";
const addOne = (num) => numn + 1;
export { addOne };

// If only export one thing from this file, use export default
export default function subtract(x, y) {
  return x - y;
}
